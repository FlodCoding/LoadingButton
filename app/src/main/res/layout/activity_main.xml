<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--  <com.flod.loadingbutton.DrawableTextView
          android:id="@+id/test3"
          android:layout_width="match_parent"
          android:layout_height="300dp"
          android:drawableStart="@drawable/ic_launcher"


          android:drawableEnd="@drawable/ic_launcher"
          android:drawableBottom="@drawable/ic_launcher"
          android:gravity="center"

          android:text="555555"
          app:drawableBottomHeight="90dp"
          app:drawableEndHeight="20dp"
          app:drawableEndWidth="100dp"

          />-->

    <!--
        <TextView
            android:layout_width="300dp"
            android:layout_height="200dp"
            android:layout_marginTop="300dp"
            android:gravity="top"
            android:text="44444444"
            android:background="@android:color/background_dark"
            android:textColor="@android:color/holo_orange_dark"
            android:drawableStart="@drawable/ic_launcher"/>-->

    <com.flod.loadingbutton.DrawableTextView
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:background="@android:color/background_dark"
        android:drawableTop="@drawable/ic_launcher"
        android:drawableBottom="@drawable/ic_launcher"
        android:gravity="center"
        android:text="登录"

        android:textColor="@android:color/holo_orange_dark" />

    <com.flod.loadingbutton.LoadingButton
        android:id="@+id/test2"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:background="@android:color/background_dark"

        android:drawableStart="@drawable/ic_launcher"
        android:drawableEnd="@drawable/ic_launcher"
        android:drawablePadding="20dp"
        android:gravity="center"

        android:text="登录"
        android:textColor="@android:color/holo_orange_dark"
        android:textSize="18sp"
        android:padding="10dp"
        app:drawableEndHeight="25dp"
        app:drawableEndWidth="25dp"
        app:enableShrink="true"
        app:endDrawable="@drawable/ic_launcher"
        app:loadingDrawablePosition="End"
        app:loadingDrawableSize="80dp" />


</FrameLayout>